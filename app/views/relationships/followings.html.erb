<h2>Follower Users</h2>
<% @users.each do |user| %>
  <div>
    <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg' %>
    <%= link_to user.name, user_path(user) %></p>
  </div>
<% end %>
<%= link_to "投稿一覧", posts_path(@post) %>
<%= paginate @posts %>