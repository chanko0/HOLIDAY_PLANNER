3
<%= link_to 'いいね数', posts_path %>
<%= link_to '最新の投稿', post_post_path(@posts) %>
<%= link_to '投稿フォーム', new_post_path %>

<% @posts.each do |post| %>
<div>
  <%= link_to post_path(post.id) do %>
    <%= attachment_image_tag post, :image, format: 'jpeg', size: "100x100"  %>
  <p>タイトル：<%= post.title %></p>
  <% end %>
  ♥<%= post.favorites.count %>いいね
</div>
<% end %>
<%= paginate @posts %>